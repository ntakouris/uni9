#include <stdio.h>
#include <math.h>

int main(void){

    int numbers = 5;
    int ints[numbers];

    for(int i = 0 ; i < numbers ;i++){
        scanf("%d", ints+i);
    }

    int occurences[numbers][numbers];

    for(int i = 0 ; i < numbers ;i++){
        occurences[numbers][numbers]
    }


    return 0;
}